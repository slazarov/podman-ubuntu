---
phase: 22-migration-docs-installability-smoke-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/apt-repository.md
  - tests/test_docs_suites.sh
autonomous: true
requirements: [MIGR-01, MIGR-03]
must_haves:
  truths:
    - "A 24.04 user can copy a DEB822 .sources block with Suites: stable-2404 from the docs and reach apt install podman-suite"
    - "A 26.04 user can copy a DEB822 .sources block with Suites: stable-2604 from the docs and reach apt install podman-suite"
    - "The GPG key import block appears once (single key, single path) and is not duplicated per distro"
    - "An existing user on bare Suites: stable sees a migration section telling them to switch to stable-2404 / stable-2604"
    - "The deprecation timeline (v3.0 deprecated, v3.1 removal, no fixed date) is documented near the top of the doc"
  artifacts:
    - path: "docs/apt-repository.md"
      provides: "Per-distro DEB822 setup, single GPG key block, deprecation callout, migration section (MIGR-01/MIGR-03)"
      contains: "stable-2604"
    - path: "tests/test_docs_suites.sh"
      provides: "Grep assertions for the six distro-qualified suite names + deprecation wording"
      contains: "stable-2404"
  key_links:
    - from: "docs/apt-repository.md deprecation callout"
      to: "docs/apt-repository.md migration section"
      via: "in-page anchor #migrating-from-bare-suite-names"
      pattern: "Migrating from Bare Suite Names"
---

<objective>
Rewrite `docs/apt-repository.md` in place so a user on either Ubuntu 24.04 or 26.04 can copy a DEB822 `.sources` block with the correct distro-qualified suite name (`stable-2404` vs `stable-2604`), document the deprecation timeline for the bare `stable`/`edge`/`nightly` names, and add a migration section for existing users. Add a doc-grep unit test asserting the new suite names and deprecation wording are present.

Purpose: MIGR-01 (per-distro copy-paste setup) and MIGR-03 (deprecation timeline + migration guide). This is the user-facing reference half of the phase; the index.html half (Plan 02) and the CI smoke gate (Plan 03) are independent.

Output: Restructured `docs/apt-repository.md` and `tests/test_docs_suites.sh`.
</objective>

<execution_context>
@/Users/slazarov/Documents/Coding/Repos/podman-debian/.claude/gsd-core/workflows/execute-plan.md
@/Users/slazarov/Documents/Coding/Repos/podman-debian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-migration-docs-installability-smoke-tests/22-CONTEXT.md
@.planning/phases/22-migration-docs-installability-smoke-tests/22-PATTERNS.md
@.planning/phases/22-migration-docs-installability-smoke-tests/22-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This phase (across all three plans) creates the following NEW symbols. Plan 01 produces the ones marked here; the rest are listed so drift-verification excludes them.

Plan 01 (this plan):
- New file `tests/test_docs_suites.sh` (doc-grep unit test)
- New doc header/anchor text "Migrating from Bare Suite Names" → GitHub slug `#migrating-from-bare-suite-names` (consumed by Plan 02's index.html deprecation callout link — SHARED CONTRACT, exact text locked)
- New per-distro section headers in `docs/apt-repository.md`: "Ubuntu 24.04 (Noble Numbat)" and "Ubuntu 26.04 (Resolute Raccoon)"
- New distro-qualified suite name strings in docs: `stable-2404`, `edge-2404`, `nightly-2404`, `stable-2604`, `edge-2604`, `nightly-2604`

Produced by sibling plans (do not implement here, listed for drift exclusion):
- Plan 02: `setDistro()` JS function, `.distro-btn` / `.distro-group` CSS classes, `data-distro` HTML attribute, `tests/test_index_html_distro.sh`
- Plan 03: `scripts/smoke_repo_install.sh`, the workflow smoke-gate step "Smoke test — install podman-suite from assembled repo per distro"
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Restructure docs/apt-repository.md to per-distro DEB822 + deprecation + migration</name>
  <files>docs/apt-repository.md</files>
  <read_first>
    - docs/apt-repository.md (current single-distro structure — read in full before editing; you will reorganize, not append)
    - .planning/phases/22-migration-docs-installability-smoke-tests/22-PATTERNS.md (section "docs/apt-repository.md" — target document structure L35-68, DEB822 block pattern L70-78, GPG key block L80-85, deprecation callout pattern L87-94, migration sed one-liner L96-102)
    - .planning/phases/22-migration-docs-installability-smoke-tests/22-UI-SPEC.md (Copywriting Contract — deprecation wording is locked verbatim by D-04/D-11)
  </read_first>
  <action>
    Reorganize the existing document into this section order (D-01): intro → deprecation callout → Ubuntu 24.04 setup → Ubuntu 26.04 setup → GPG Signing Key → Track Selection → Individual Packages → Supported Architectures → Migrating from Bare Suite Names → Troubleshooting → Important Notes.

    Specific edits:
    1. Intro paragraph: update to state the repo serves both Ubuntu 24.04 (Noble Numbat) and Ubuntu 26.04 (Resolute Raccoon) LTS. Remove "serves two suites: stable / edge" framing — the suites are now distro-qualified.
    2. Add a deprecation callout as a `> **Note:**` Markdown blockquote (D-05/D-12) immediately after the intro, near the top of the doc so existing users see it first. Quote this wording VERBATIM (locked by D-04/D-11): "Deprecated in v3.0 (June 2026). Bare suite names will be removed in a future v3.1 release. Monitor the changelog or watch the GitHub repository for the removal notice." The callout MUST link to the migration section via the relative anchor `#migrating-from-bare-suite-names`.
    3. Create two parallel per-distro setup subsections with headers exactly "## Ubuntu 24.04 (Noble Numbat)" and "## Ubuntu 26.04 (Resolute Raccoon)" (discretion D-01; these exact strings are also asserted by the test). Each subsection holds one DEB822 block. The 24.04 block uses `Suites: stable-2404`; the 26.04 block uses `Suites: stable-2604` (D-02). Every block keeps `Types: deb`, `URIs: https://slazarov.github.io/podman-ubuntu`, `Components: main`, `Signed-By: /etc/apt/keyrings/podman-ubuntu.gpg` identical across distros (D-06). Mention `edge-2404`/`nightly-2404` and `edge-2604`/`nightly-2604` as the per-distro values to swap into the `Suites:` line for other tracks.
    4. GPG Signing Key: keep the existing key-download block (mkdir /etc/apt/keyrings + wget the .gpg) exactly once, not per-distro (D-06).
    5. Migration section header MUST be exactly "## Migrating from Bare Suite Names" (this produces the GitHub slug `#migrating-from-bare-suite-names` that the deprecation callout AND Plan 02's index.html callout both link to — SHARED CONTRACT, do not reword). Content (D-03): tell existing users with `Suites: stable` (or edge/nightly) to change to `stable-2404` (24.04) or `stable-2604` (26.04). Provide both an exact sed one-liner (`sudo sed -i 's/Suites: stable$/Suites: stable-2404/' /etc/apt/sources.list.d/podman-ubuntu.sources`) and the full replacement `.sources` block to paste. State explicitly that bare suites continue serving 24.04 packages during the deprecation window.
    6. Troubleshooting "Repository returns 404" subsection: update the suite-list line so the valid `Suites` value mentions the distro-qualified names (stable-2404 / stable-2604 etc.), not bare `stable` or `edge`.
    7. Important Notes: update the suffix note from `~podman1` to `~ubuntu24.04.podman1` / `~ubuntu26.04.podman1` (D-05, per Phase 19). Mention Ed25519 key is supported by Ubuntu 24.04 and later.

    SECURITY (Tampering boundary, T-22-DOC-01): `[trusted=yes]` / `Trusted: yes` must NEVER appear anywhere in this doc. It is a CI-internal smoke-test shortcut only. Every user-facing snippet uses `Signed-By: /etc/apt/keyrings/podman-ubuntu.gpg`.

    The bare names `stable`/`edge`/`nightly` appear ONLY in the deprecation callout and the migration section — never as the primary setup path (D-02). Keep existing Individual Packages table and Supported Architectures content; update Supported Architectures to mention both Ubuntu LTS versions.
  </action>
  <verify>
    <automated>bash tests/test_docs_suites.sh</automated>
  </verify>
  <acceptance_criteria>
    - docs/apt-repository.md contains the string "Suites: stable-2404"
    - docs/apt-repository.md contains the string "Suites: stable-2604"
    - docs/apt-repository.md contains "edge-2404", "edge-2604", "nightly-2404", "nightly-2604"
    - docs/apt-repository.md contains a header line exactly "## Migrating from Bare Suite Names"
    - docs/apt-repository.md contains the verbatim string "Bare suite names will be removed in a future v3.1 release"
    - docs/apt-repository.md contains "Signed-By: /etc/apt/keyrings/podman-ubuntu.gpg"
    - docs/apt-repository.md does NOT contain "trusted=yes" and does NOT contain "Trusted: yes" (grep -c returns 0)
    - docs/apt-repository.md contains "~ubuntu24.04.podman1" and "~ubuntu26.04.podman1"
    - The deprecation callout links to "#migrating-from-bare-suite-names"
  </acceptance_criteria>
  <done>
    docs/apt-repository.md is restructured with per-distro DEB822 blocks, a single GPG key block, a top-of-doc deprecation callout linking to the migration section, and a migration section with sed one-liner + full .sources block. No `trusted=yes` appears anywhere. `bash tests/test_docs_suites.sh` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests/test_docs_suites.sh doc-grep unit test</name>
  <files>tests/test_docs_suites.sh</files>
  <read_first>
    - .planning/phases/22-migration-docs-installability-smoke-tests/22-PATTERNS.md (section "tests/test_docs_suites.sh" L357-384 — the assert_contains skeleton to follow)
    - tests/test_detect_distro_depends.sh (existing test harness idiom: set -euo pipefail, PASS/FAIL counters, exit non-zero on any failure)
    - docs/apt-repository.md (the file under assertion — after Task 1)
  </read_first>
  <action>
    Create `tests/test_docs_suites.sh` as a standalone Bash test following the existing `tests/` harness idiom (`#!/bin/bash`, `set -euo pipefail`, an `assert_contains file pattern label` helper using `grep -qF`, PASS/FAIL counters, final `[[ $FAIL -eq 0 ]]` exit). Resolve the doc path relative to the script: `DOC="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/docs/apt-repository.md"`.

    Assert the doc contains: `stable-2404`, `stable-2604`, `edge-2404`, `edge-2604`, `nightly-2404`, `nightly-2604`, `Signed-By: /etc/apt/keyrings/podman-ubuntu.gpg`, the header text `Migrating from Bare Suite Names`, and the verbatim deprecation phrase `Bare suite names will be removed in a future v3.1 release`.

    Add a NEGATIVE assertion that `trusted=yes` does NOT appear in the doc: `if grep -q "trusted=yes" "$DOC"; then echo "FAIL: trusted=yes leaked into user-facing docs"; FAIL++; fi` (T-22-DOC-01 — security boundary).

    This test is LOCAL/MANUAL only — it is not wired into CI (per RESEARCH Wave 0 gap: tests/ are not run by build-packages.yml). Add a one-line header comment noting it runs via `bash tests/test_docs_suites.sh` and is not auto-run in CI.
  </action>
  <verify>
    <automated>bash tests/test_docs_suites.sh</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_docs_suites.sh exists and starts with "#!/bin/bash"
    - tests/test_docs_suites.sh contains "set -euo pipefail"
    - tests/test_docs_suites.sh asserts "stable-2404" and "stable-2604" presence
    - tests/test_docs_suites.sh contains a negative assertion for "trusted=yes"
    - `bash tests/test_docs_suites.sh` exits 0 against the Task-1 doc
    - `bash -n tests/test_docs_suites.sh` passes (syntax check)
  </acceptance_criteria>
  <done>
    `bash tests/test_docs_suites.sh` exits 0, asserting all six distro-qualified suite names, the Signed-By path, the migration header, the verbatim deprecation phrase, and that `trusted=yes` is absent from the docs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → user shell | Users copy-paste snippets from the rendered docs into a root shell; a snippet that disables signature verification would weaken every reader's apt trust chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-DOC-01 | Tampering / Spoofing | docs/apt-repository.md user-facing snippets | mitigate | Every user-facing block uses `Signed-By: /etc/apt/keyrings/podman-ubuntu.gpg`; `trusted=yes`/`Trusted: yes` must NEVER appear (Task 2 adds a negative grep assertion enforcing this) |
| T-22-DOC-02 | Information disclosure | deprecation/migration wording | accept | Public repo URL + suite names are already public; no secret material in docs |

No npm/pip/cargo installs in this plan — no `T-22-SC` package-legitimacy checkpoint required (RESEARCH: no external packages this phase).
</threat_model>

<verification>
- `bash tests/test_docs_suites.sh` exits 0 (asserts all six suite names, Signed-By path, migration header, verbatim deprecation phrase, no trusted=yes)
- `bash -n tests/test_docs_suites.sh` passes
- Manual: GitHub-rendered Markdown shows two parallel per-distro subsections and a single GPG key block (rendering verification deferred to PR preview — content correctness is covered by the grep test)
</verification>

<success_criteria>
- A 24.04 user copies the `stable-2404` DEB822 block and a 26.04 user copies the `stable-2604` block; both reach `apt install podman-suite` (MIGR-01)
- The deprecation timeline (v3.0 deprecated, v3.1 removal, no fixed date) and a migration section with sed one-liner + full .sources block are present (MIGR-03)
- The GPG key path/import is documented once and identical across distros (ROADMAP SC-4)
- No `trusted=yes` leaks into user-facing docs
</success_criteria>

<output>
Create `.planning/phases/22-migration-docs-installability-smoke-tests/22-01-SUMMARY.md` when done
</output>
