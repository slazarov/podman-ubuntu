---
phase: 22-migration-docs-installability-smoke-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke_repo_install.sh
  - .github/workflows/build-packages.yml
autonomous: true
requirements: [MIGR-04]
must_haves:
  truths:
    - "A helper script installs podman-suite (by name) from the assembled repo-output via a file:// APT source inside a real ubuntu container and runs podman info"
    - "The CI publish job runs the smoke gate for both distros after both ci_publish.sh runs and before the GitHub Pages upload"
    - "If either container's apt install or podman info fails, the workflow fails at that step and the Pages upload does not run"
    - "The failure message names which distro/suite failed"
    - "The 26.04 leg falls back to ubuntu:resolute if ubuntu:26.04 is not pullable"
  artifacts:
    - path: "scripts/smoke_repo_install.sh"
      provides: "MIGR-04 smoke helper: file:// repo install of podman-suite + podman info, per distro, runtime/image validated"
      contains: "podman info"
    - path: ".github/workflows/build-packages.yml"
      provides: "Smoke-gate step in the publish job between the publish step and the Pages upload (MIGR-04)"
      contains: "smoke_repo_install.sh"
  key_links:
    - from: ".github/workflows/build-packages.yml publish job"
      to: "scripts/smoke_repo_install.sh"
      via: "step invoking the helper twice (2404, 2604) with TRACK + repo-output"
      pattern: "smoke_repo_install.sh"
    - from: "scripts/smoke_repo_install.sh"
      to: "assembled repo-output (file:///opt/podman-repo)"
      via: "container bind-mount + DEB822 file:// source"
      pattern: "file:///opt/podman-repo"
---

<objective>
Add a CI installability smoke gate (MIGR-04). Create `scripts/smoke_repo_install.sh`: a helper that, for a given distro label, spins up a real `ubuntu:<distro>` container, points it at the assembled `repo-output` via a `file://` APT source with `[trusted=yes]` (CI-internal only), installs the `podman-suite` meta-package BY NAME, and runs `podman info`. Wire it into the `publish` job in `.github/workflows/build-packages.yml` — running both distro legs after both `ci_publish.sh` runs and before the GitHub Pages upload, so an uninstallable package never reaches the live repo.

Purpose: MIGR-04 (CI verifies install + `podman info` in real ubuntu:24.04 and ubuntu:26.04 containers before publish). The gate proves installability + runnability; because of `[trusted=yes]` it does NOT exercise the GPG Signed-By path (accepted limitation, D-14).

Output: New `scripts/smoke_repo_install.sh` and an edited `.github/workflows/build-packages.yml`.
</objective>

<execution_context>
@/Users/slazarov/Documents/Coding/Repos/podman-debian/.claude/gsd-core/workflows/execute-plan.md
@/Users/slazarov/Documents/Coding/Repos/podman-debian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-migration-docs-installability-smoke-tests/22-CONTEXT.md
@.planning/phases/22-migration-docs-installability-smoke-tests/22-PATTERNS.md
@.planning/phases/22-migration-docs-installability-smoke-tests/22-RESEARCH.md
@scripts/smoke_install_2604.sh
</context>

<artifacts_this_phase_produces>
Plan 03 (this plan) creates these NEW symbols:
- New file `scripts/smoke_repo_install.sh` (MIGR-04 smoke helper; takes a distro label + repo-dir arg)
- New workflow step in the `publish` job named "Smoke test — install podman-suite from assembled repo per distro"

Produced by sibling plans (listed for drift exclusion, do not implement here): Plan 01 `docs/apt-repository.md` rewrite + `tests/test_docs_suites.sh`; Plan 02 `setDistro()`/`.distro-btn`/`data-distro` in `scripts/ci_publish.sh` + `tests/test_index_html_distro.sh`.

Existing symbols this plan REUSES (not new — do not flag): `SMOKE_RUNTIME` env override + runtime validation block (from `smoke_install_2604.sh`), the `ubuntu:26.04`→`ubuntu:resolute` image fallback, `${{ steps.track.outputs.track }}` (the `track` step output), `repo-output` (assembled repo dir in the publish job), the toolpath bootstrap idiom.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/smoke_repo_install.sh (file:// install of podman-suite + podman info)</name>
  <files>scripts/smoke_repo_install.sh</files>
  <read_first>
    - scripts/smoke_install_2604.sh (READ IN FULL — the prior-art smoke idiom: header+usage block, toolpath bootstrap, SMOKE_RUNTIME validation L50-73, ubuntu:26.04→resolute image fallback L82-107, --rm bind-mount container invocation L146-150, hard-fail pass/fail messages. NOTE the key difference below)
    - .planning/phases/22-migration-docs-installability-smoke-tests/22-PATTERNS.md (section "scripts/smoke_repo_install.sh" L255-353 — header, toolpath bootstrap, runtime selection, image fallback, container invocation, hard-fail patterns to copy)
    - .planning/phases/22-migration-docs-installability-smoke-tests/22-RESEARCH.md (Pattern 3 container invocation L182-196; Pitfall 1 VFS fallback L216-223; Security Domain command-injection mitigation L400)
  </read_first>
  <action>
    Create `scripts/smoke_repo_install.sh`. KEY DIFFERENCE FROM THE ANALOG: `smoke_install_2604.sh` installs a LOCAL `.deb` by path from `${toolpath}/output`. This helper installs the META-PACKAGE `podman-suite` BY NAME from the ASSEMBLED repo (`repo-output`, the 9-suite signed tree) via a `file://` APT source. Different directory, different install mechanism — do NOT copy the `.deb`-by-path install line.

    Structure:
    1. Header: `#!/bin/bash`, `set -euo pipefail`, a usage comment block. Usage: `smoke_repo_install.sh <distro-label> [repo-dir]` where distro-label is `2404` or `2604` (required) and repo-dir defaults to `${toolpath}/repo-output` (the assembled repo). Document the `SMOKE_RUNTIME=docker|podman` override (validated) and `TRACK` env var (the suite track; default `nightly` to mirror the workflow default).
    2. Toolpath bootstrap: copy the realpath bootstrap idiom from `smoke_install_2604.sh` L31-36 (quoted). Do NOT source config.sh/functions.sh unless needed — this helper must run standalone in a minimal env (CI runner) and the analog does not source them either; keep it self-contained.
    3. Validate the distro-label arg: it MUST be exactly `2404` or `2604` (case statement, exact match — it is interpolated into the suite name and image tag, T-22-SMOKE-01). Reject anything else with a clear error + exit 1. Derive `SUITE="${TRACK:-nightly}-${label}"`.
    4. Runtime selection + validation: copy the `SMOKE_RUNTIME` block from `smoke_install_2604.sh` L50-73 verbatim (exact-match `docker|podman` whitelist before interpolation — injection-safe).
    5. Image selection: for `2404` use `ubuntu:24.04`; for `2604` use the `ubuntu:26.04`→`ubuntu:resolute` pull-fallback loop (copy from L82-107). Validate any image string before use (the analog's regex L84 if an override path is added).
    6. Resolve REPO_DIR: arg $2 if given, else `${toolpath}/repo-output`. Hard-fail with a clear message if REPO_DIR does not exist or contains no `dists/` tree.
    7. Container invocation: `"${RUNTIME}" run --rm --privileged --device /dev/fuse -v "${REPO_DIR}:/opt/podman-repo:ro" -e DEBIAN_FRONTEND=noninteractive "${IMAGE}" bash -s` and feed the install script via STDIN heredoc (`bash -s << INNEREOF` ... `INNEREOF`) to avoid the nested single-quote hazard. Inside the container script (runs with `set -e`): write `/etc/apt/sources.list.d/podman-smoke.sources` as a DEB822 source — `Types: deb`, `URIs: file:///opt/podman-repo`, `Suites: ${SUITE}` (interpolated from the outer shell), `Components: main`, `Trusted: yes` (CI-internal `[trusted=yes]` equivalent — see security note); then `apt-get update -qq`; `apt-get install -y -q podman-suite`; `podman info --log-level=error`. `podman info` exit 0 is the gate (D-15).
    8. VFS fallback note (RESEARCH Pitfall 1): if `podman info` errors on storage in real CI, the documented mitigation is to write `/etc/containers/storage.conf` with `[storage]\ndriver = "vfs"` before `podman info`. Include this as a commented-out block in the container script with a one-line comment explaining when to enable it — do NOT enable it pre-emptively (only if CI proves the storage probe fails).
    9. On any failure (the `if ! "${RUNTIME}" run ...; then` guard), print `SMOKE FAIL: ${IMAGE} — install or podman info failed for suite ${SUITE}` to stderr and `exit 1`. On success print a `>>> SMOKE PASS: ${IMAGE} suite=${SUITE}` line and `exit 0`.

    SECURITY (T-22-SMOKE-01, command injection): the distro-label and `SMOKE_RUNTIME` are interpolated into a `docker/podman run` invocation. Both are exact-match-validated before use (label ∈ {2404,2604}; runtime ∈ {docker,podman}). `TRACK` comes from a constrained CI input. `Trusted: yes` is intentional here and CONFINED to this CI-internal `file://` smoke source — it must never appear in user-facing docs/index.html (those are Plans 01/02; this boundary is documented so a future edit never copies this snippet outward).

    DEV-HOST CONSTRAINT: this is a macOS dev host — author and syntax-check only (`bash -n scripts/smoke_repo_install.sh`). Real execution is deferred to CI or a Lima VM (ubuntu-24 / ubuntu-26, repo mounted at /opt/podman-debian), matching the Phase 19 pattern. Do NOT attempt to run the container here.
  </action>
  <verify>
    <automated>bash -n scripts/smoke_repo_install.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/smoke_repo_install.sh exists, starts with "#!/bin/bash", contains "set -euo pipefail"
    - `bash -n scripts/smoke_repo_install.sh` exits 0
    - The script validates the distro-label arg against an exact-match {2404,2604} whitelist (case statement present)
    - The script reuses the SMOKE_RUNTIME exact-match docker|podman validation
    - The script contains the ubuntu:26.04 → ubuntu:resolute fallback for the 2604 leg
    - The container install line is `apt-get install -y -q podman-suite` (meta-package by NAME), NOT a `.deb`-by-path install
    - The container script contains "podman info --log-level=error"
    - The DEB822 file:// source contains "URIs: file:///opt/podman-repo" and "Suites: ${SUITE}" (or the interpolated equivalent)
    - The container run uses "--privileged --device /dev/fuse" and "-v ${REPO_DIR}:/opt/podman-repo:ro"
    - On failure the script prints a message naming the distro/suite and exits non-zero
  </acceptance_criteria>
  <done>
    `bash -n scripts/smoke_repo_install.sh` exits 0. The helper validates its distro label, selects+validates the runtime, falls back to resolute for 26.04, bind-mounts repo-output read-only, installs podman-suite by name from a file:// DEB822 source, runs podman info, and hard-fails (naming the distro/suite) if either step fails. Execution deferred to CI/Lima.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the smoke gate into the publish job in build-packages.yml</name>
  <files>.github/workflows/build-packages.yml</files>
  <read_first>
    - .github/workflows/build-packages.yml (publish job L276-368 — the `track` step output L286-291, the `Build and publish repository` step L328-355 ending at `publish_distro "2604" "all-debs-2604"` L355, then `actions/configure-pages@v4` L357, `upload-pages-artifact@v3` L362-364, `deploy-pages@v4` L366-368)
    - .planning/phases/22-migration-docs-installability-smoke-tests/22-PATTERNS.md (section ".github/workflows/build-packages.yml" L199-251 — insertion point L203, step structure, the inline-step idiom, TRACK reference L452-457)
    - scripts/smoke_repo_install.sh (the helper from Task 1 — the workflow invokes it)
  </read_first>
  <action>
    Insert ONE new step into the `publish` job, AFTER the `Build and publish repository` step (which ends with `publish_distro "2604" "all-debs-2604"` at L355) and BEFORE the `- uses: actions/configure-pages@v4` step (L357). This placement (D-13) tests the actual assembled `repo-output` before the Pages upload — if smoke fails, `configure-pages`/`upload-pages-artifact`/`deploy-pages` never run, so the live repo is untouched (D-16).

    The new step `name:` is exactly "Smoke test — install podman-suite from assembled repo per distro". Its `run:` block (inline shell, `set -euo pipefail`): set `TRACK="${{ steps.track.outputs.track }}"` (mirrors the publish step's reference) and export it so the helper sees it; then invoke the Task-1 helper twice against the assembled repo: `./scripts/smoke_repo_install.sh 2404 "$PWD/repo-output"` and `./scripts/smoke_repo_install.sh 2604 "$PWD/repo-output"`. Each call hard-fails the step on non-zero (the helper exits 1 on failure; with `set -e` in the step the workflow aborts there). Echo a `>>> Running smoke gate for track ${TRACK}` line before the calls for log clarity.

    Do NOT add a separate `run-tests` CI job and do NOT wire the new tests/*.sh into CI — that is out of locked scope (CONTEXT scopes this phase to docs + ci_publish.sh + the smoke gate; RESEARCH Wave 0 gap notes tests/ are local-only and flags adding a run-tests job as a scope expansion to avoid). Only the smoke gate is added to the workflow.

    Keep the existing `publish` job structure otherwise unchanged — the smoke step slots between the publish step and configure-pages with no change to `needs`, `if`, or the artifact upload.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/build-packages.yml')); print('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/build-packages.yml parses as valid YAML (python3 yaml.safe_load succeeds)
    - The file contains a step named "Smoke test — install podman-suite from assembled repo per distro"
    - That step invokes "./scripts/smoke_repo_install.sh 2404" and "./scripts/smoke_repo_install.sh 2604"
    - The step references `${{ steps.track.outputs.track }}` for TRACK
    - The smoke step appears AFTER the line `publish_distro "2604" "all-debs-2604"` and BEFORE the line `uses: actions/configure-pages@v4` (grep line-number ordering confirms: smoke step line number > publish_distro 2604 line, < configure-pages line)
    - No new `run-tests` job is added (publish job still has needs: [build] only; no second job introduced)
  </acceptance_criteria>
  <done>
    The publish job runs the smoke gate (both distro legs via scripts/smoke_repo_install.sh against repo-output) between the publish step and the Pages upload. A smoke failure aborts the job before any Pages deploy. YAML parses cleanly. No run-tests job added.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI matrix input (TRACK) + distro label → docker/podman run invocation | The track and distro label are interpolated into a container-run command and a suite name |
| assembled repo-output → smoke container | The repo is bind-mounted read-only into the container; `[trusted=yes]` disables signature checking for the CI-internal test only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-SMOKE-01 | Tampering (command injection) | interpolated distro label / SMOKE_RUNTIME in smoke_repo_install.sh | mitigate | Distro label exact-match-validated against {2404,2604}; SMOKE_RUNTIME exact-match {docker,podman} (copied from smoke_install_2604.sh) before any interpolation; TRACK is a constrained CI input |
| T-22-SMOKE-02 | Spoofing / Tampering | `[trusted=yes]` / `Trusted: yes` smoke source disables GPG verification | accept | Confined to the CI-internal `file://` smoke test (D-14); proves installability + runnability only, NOT the Signed-By path real users hit (accepted limitation, recorded). MUST never appear in user-facing docs/index.html (enforced in Plans 01/02) |
| T-22-SMOKE-03 | Denial of service | uninstallable package reaching the live repo | mitigate | The gate runs BEFORE the Pages upload (D-13); a failed install or `podman info` aborts the job, leaving the live repository untouched (D-16) |

No npm/pip/cargo installs in this plan — `podman-suite` is a project-internal build artifact, `ubuntu:24.04`/`ubuntu:26.04` are official Docker Hub base images (not language-ecosystem packages). No `T-22-SC` package-legitimacy checkpoint required (RESEARCH: N/A — no external packages).
</threat_model>

<verification>
- `bash -n scripts/smoke_repo_install.sh` exits 0
- `.github/workflows/build-packages.yml` parses as valid YAML (python3 yaml.safe_load)
- grep confirms the smoke step sits between `publish_distro "2604"` and `actions/configure-pages@v4`
- END-TO-END (deferred to CI / Lima per dev-host constraint): a real publish run (or `bash scripts/smoke_repo_install.sh 2404 <repo-output>` in ubuntu-24, `... 2604` in ubuntu-26) shows `apt install podman-suite` + `podman info` exit 0 in both containers. `/gsd-verify-work` MUST treat MIGR-04 as CI-proven, not macOS-proven.
</verification>

<success_criteria>
- CI installs `podman-suite` and runs `podman info` in real ubuntu:24.04 and ubuntu:26.04 containers before any publish (MIGR-04)
- The smoke gate runs after repo assembly and before the Pages upload; a failure aborts the job and leaves the live repo untouched
- The failure message names the failing distro/suite; the 26.04 leg falls back to resolute if needed
- `[trusted=yes]` stays confined to the CI-internal smoke source — never user-facing
</success_criteria>

<output>
Create `.planning/phases/22-migration-docs-installability-smoke-tests/22-03-SUMMARY.md` when done
</output>
