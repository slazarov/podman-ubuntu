---
phase: 19-per-distro-versioning-dependency-mapping
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_versions.sh
autonomous: true
requirements: [PKG-09]
must_haves:
  truths:
    - "A suffixed version sorts below the plain upstream version (yields to official)"
    - "The 24.04 suffix form sorts below the 26.04 suffix form of the same upstream version"
    - "A nightly form (D-09: {base}~git{date}.{sha}~ubuntu{VERSION_ID}.podman1) sorts below the tagged-release form for the same distro"
    - "The legacy ~podman1 form sorts below the new ~ubuntu24.04.podman1 form so existing installs upgrade cleanly"
    - "Running scripts/verify_versions.sh exits 0 when all orderings hold and non-zero on any violation"
  artifacts:
    - path: "scripts/verify_versions.sh"
      provides: "dpkg --compare-versions assertions for the per-distro suffix ordering (D-11), including the D-09 nightly form and D-10 pasta date form"
      contains: "dpkg --compare-versions"
  key_links:
    - from: "scripts/verify_versions.sh"
      to: "dpkg --compare-versions"
      via: "assert_lt wrapper"
      pattern: "dpkg --compare-versions"
---

<objective>
Create the CI-agnostic version-ordering proof: `scripts/verify_versions.sh` asserts with `dpkg --compare-versions` that the per-distro suffix form sorts correctly (D-11) — yields to official upstream, 24.04 < 26.04, the D-09 nightly form < tagged, and legacy `~podman1` < new `~ubuntu24.04.podman1`. This closes the STATE.md v3.0 research flag for Phase 19 ("confirm exact version suffix form with dpkg --compare-versions before shipping").

Purpose: PKG-09 demands distinct, correctly-sortable per-distro version identities. `dpkg --compare-versions` is the authoritative oracle (no reimplemented version math). This script is the fast, dpkg-only test that runs on any host with dpkg — independent of the build pipeline.

Output: `scripts/verify_versions.sh` (new), executable, with the mandatory D-11 ordering assertions (covering the D-09 nightly and D-10 pasta-date forms) wired into exit status.
</objective>

<execution_context>
@/Users/slazarov/Documents/Coding/Repos/podman-debian/.claude/gsd-core/workflows/execute-plan.md
@/Users/slazarov/Documents/Coding/Repos/podman-debian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-per-distro-versioning-dependency-mapping/19-CONTEXT.md
@.planning/phases/19-per-distro-versioning-dependency-mapping/19-RESEARCH.md
@.planning/phases/19-per-distro-versioning-dependency-mapping/19-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift verification — they are new):
- `scripts/verify_versions.sh` — new file
- `assert_lt()` — helper function inside verify_versions.sh

No symbols consumed from other plans (this plan is self-contained; the version strings are literal test fixtures, not derived from config.sh at runtime).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/verify_versions.sh with the D-11 ordering assertions (D-09 nightly + D-10 pasta forms)</name>
  <files>scripts/verify_versions.sh</files>
  <read_first>
    - scripts/package_all.sh (lines 1-17 — copy the standard script header: `#!/bin/bash`, `set -euo pipefail`, toolpath resolution if needed, ERR trap pattern. This new script should match that house style)
    - .planning/phases/19-per-distro-versioning-dependency-mapping/19-RESEARCH.md (Code Examples lines 318-333 — the assert_lt wrapper and the exact four assertions; Pitfall 2 lines 294-302 — the four orderings and why; Runtime State Inventory lines 279 — the legacy ~podman1 < new form reasoning)
    - .planning/phases/19-per-distro-versioning-dependency-mapping/19-PATTERNS.md (lines 136-138 — pattern source and the four-assertion requirement)
    - .planning/phases/19-per-distro-versioning-dependency-mapping/19-CONTEXT.md (D-08 suffix form, D-09 nightly form, D-10 pasta form, D-11 assertion list)
    - .planning/codebase/CONVENTIONS.md (Error Handling + strict-mode sections — match `set -euo pipefail`, stderr errors, 2-space indent)
  </read_first>
  <action>
    Create scripts/verify_versions.sh as a self-contained, dpkg-only verification script. Header: `#!/bin/bash` + `set -euo pipefail`. Make it executable (mode 0755).

    Define `assert_lt()` taking two version strings: if `dpkg --compare-versions "$1" lt "$2"` succeeds, echo `OK: $1 < $2`; else echo `FAIL: expected $1 < $2` to stderr and `exit 1`. The four mandatory orderings are all `lt`, so assert_lt suffices.

    Wire these four hard assertions (RESEARCH Code Examples + Pitfall 2 + Runtime State Inventory + D-11), using realistic representative versions:
    1. `assert_lt "5.5.2~ubuntu24.04.podman1" "5.5.2"` — D-08 suffixed form yields to official upstream.
    2. `assert_lt "5.5.2~ubuntu24.04.podman1" "5.5.2~ubuntu26.04.podman1"` — 24.04 sorts below 26.04 (dist-upgrade order).
    3. `assert_lt "5.9.0~git20260306.abc1234~ubuntu24.04.podman1" "5.9.0~ubuntu24.04.podman1"` — the D-09 nightly form (`{base}~git{YYYYMMDD}.{sha}~ubuntu{VERSION_ID}.podman1`) sorts below the tagged release for the same distro.
    4. `assert_lt "5.5.2~podman1" "5.5.2~ubuntu24.04.podman1"` — legacy already-published form sorts below the new per-distro form, so existing installs upgrade cleanly (Runtime State Inventory).

    Also add, for symmetry/robustness: the 26.04 nightly < 26.04 tagged ordering (`5.9.0~git20260306.abc1234~ubuntu26.04.podman1` lt `5.9.0~ubuntu26.04.podman1`) so the D-09 nightly form is proven for both distros, and the pasta-style date base form (D-10: `20250302~ubuntu24.04.podman1` lt `20250302~ubuntu26.04.podman1`) since pasta carries the same distro suffix on a date base.

    On success, print a final `All version ordering assertions passed` line and `exit 0`. Because `set -euo pipefail` is on and every failed assertion `exit 1`s, the script's exit status is the verification result. Do NOT depend on config.sh or any build output — the version strings are literal fixtures so the script runs anywhere dpkg exists (including in CI before any build).
  </action>
  <behavior>
    - With all six orderings holding (they do, per dpkg tilde semantics), the script exits 0 and prints OK lines + the final pass line.
    - If any assert_lt is given a pair that violates the ordering, that assertion prints FAIL to stderr and the script exits 1.
    - The script needs only `dpkg` (no nfpm, no build, no config.sh).
  </behavior>
  <verify>
    <automated>bash -n scripts/verify_versions.sh && test -x scripts/verify_versions.sh && grep -q 'dpkg --compare-versions' scripts/verify_versions.sh && grep -c 'assert_lt ' scripts/verify_versions.sh | { read n; [ "$n" -ge 4 ] && echo OK || { echo "FAIL: only $n assertions"; exit 1; }; }</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify_versions.sh` exits 0
    - `scripts/verify_versions.sh` is executable (`test -x` passes)
    - File contains `set -euo pipefail`
    - File contains `dpkg --compare-versions` inside an `assert_lt` helper
    - At least 4 `assert_lt ` invocations present (the four mandatory D-11 orderings)
    - Contains the literal assertion `assert_lt "5.5.2~ubuntu24.04.podman1" "5.5.2~ubuntu26.04.podman1"` (24.04 < 26.04)
    - Contains a D-09 nightly-form assertion: `assert_lt "5.9.0~git20260306.abc1234~ubuntu24.04.podman1" "5.9.0~ubuntu24.04.podman1"`
    - Contains the legacy-upgrade assertion `assert_lt "5.5.2~podman1" "5.5.2~ubuntu24.04.podman1"`
    - On a host with dpkg: `bash scripts/verify_versions.sh` exits 0 (all orderings hold)
  </acceptance_criteria>
  <done>
    scripts/verify_versions.sh exists, executable, self-contained (dpkg-only), with assert_lt wrapping `dpkg --compare-versions ... lt ...` and the four mandatory D-11 orderings (including the D-09 nightly form and the D-10 pasta-date symmetry). `bash -n` clean; exits 0 where dpkg is present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none external) | verify_versions.sh operates on literal in-script version fixtures and the host dpkg binary only; no untrusted input, no network, no filesystem writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Tampering | version-ordering oracle | accept | The script uses the host's `dpkg --compare-versions` as the authoritative oracle and only literal fixtures; there is no external input surface and no reimplemented version math to get wrong. Risk is limited to a wrong fixture, which is caught by the assertion semantics themselves. |
</threat_model>

<verification>
- `bash -n scripts/verify_versions.sh` passes; file executable; contains `set -euo pipefail`.
- The four mandatory D-11 `assert_lt` orderings present, including the 24.04<26.04, the D-09 nightly<tagged, and legacy<new assertions.
- On any dpkg host, `bash scripts/verify_versions.sh` exits 0 (this is runnable in CI and locally wherever dpkg exists; on the macOS dev host dpkg is absent, so the exit-0 proof runs on an Ubuntu host / container — the syntax + structural greps run anywhere).
</verification>

<success_criteria>
- scripts/verify_versions.sh proves, via the dpkg oracle, all four mandatory orderings (yield-to-official, 24.04<26.04, D-09 nightly<tagged, legacy<new) plus the D-10 pasta-date symmetry, and exits non-zero on any violation — closing the Phase 19 STATE.md research flag (PKG-09).
</success_criteria>

<output>
Create `.planning/phases/19-per-distro-versioning-dependency-mapping/19-03-SUMMARY.md` when done.
</output>
