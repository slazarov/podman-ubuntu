---
phase: 19-per-distro-versioning-dependency-mapping
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - scripts/package_all.sh
  - packaging/nfpm/podman.yaml
  - packaging/nfpm/buildah.yaml
  - packaging/nfpm/skopeo.yaml
  - packaging/nfpm/crun.yaml
  - packaging/nfpm/conmon.yaml
  - packaging/nfpm/pasta.yaml
autonomous: true
requirements: [PKG-08, PKG-10]
must_haves:
  truths:
    - "package_all.sh derives each component's system-library depends from its binaries at build time via detect_runtime_depends"
    - "The hardcoded VERSION_SUFFIX line in package_all.sh is gone; the per-distro VERSION_SUFFIX from config.sh is appended in the packaging loop"
    - "detect_crun_parser_depend and the CRUN_PARSER_DEPEND special case are fully removed"
    - "Every nFPM YAML that carried hardcoded system-lib deps now injects ${DETECTED_DEPENDS} and keeps only internal podman-* deps literal"
  artifacts:
    - path: "scripts/package_all.sh"
      provides: "per-component detection + ${DETECTED_DEPENDS} injection into envsubst pipeline"
      contains: "detect_runtime_depends"
    - path: "packaging/nfpm/crun.yaml"
      provides: "depends block driven by ${DETECTED_DEPENDS}"
      contains: "${DETECTED_DEPENDS}"
  key_links:
    - from: "scripts/package_all.sh"
      to: "functions.sh::detect_runtime_depends"
      via: "per-component call on DESTDIR binaries"
      pattern: "detect_runtime_depends"
    - from: "scripts/package_all.sh"
      to: "packaging/nfpm/*.yaml"
      via: "envsubst with ${DETECTED_DEPENDS} in the allowlist"
      pattern: "DETECTED_DEPENDS"
---

<objective>
Wire the Plan 01 helpers into the packaging pipeline: replace hardcoded `VERSION_SUFFIX="~podman1"` with the per-distro value from config.sh, delete the crun special case (`detect_crun_parser_depend` + `CRUN_PARSER_DEPEND`), compute each component's system-library depends with `detect_runtime_depends`, and inject them as a `${DETECTED_DEPENDS}` YAML fragment into the nFPM configs. nFPM YAMLs keep internal `podman-*` deps literal (D-13) and drop all hardcoded system libs (D-12).

Purpose: Make .deb dependency declarations ground-truth-derived (PKG-10) so 26.04 renames (libgpgme45, libsubid5) and 24.04 t64 names are picked up automatically (PKG-08), and let per-distro version identity flow from the single source of truth (PKG-09 plumbing; ordering proof is Plan 03).

Output: `scripts/package_all.sh` rewired; 6 nFPM YAMLs converted to `${DETECTED_DEPENDS}` injection.
</objective>

<execution_context>
@/Users/slazarov/Documents/Coding/Repos/podman-debian/.claude/gsd-core/workflows/execute-plan.md
@/Users/slazarov/Documents/Coding/Repos/podman-debian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-per-distro-versioning-dependency-mapping/19-CONTEXT.md
@.planning/phases/19-per-distro-versioning-dependency-mapping/19-RESEARCH.md
@.planning/phases/19-per-distro-versioning-dependency-mapping/19-PATTERNS.md
@.planning/phases/19-per-distro-versioning-dependency-mapping/19-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift verification — they are new):
- `${DETECTED_DEPENDS}` — nFPM YAML placeholder injected in crun/podman/buildah/skopeo/conmon/pasta YAMLs
- `DETECTED_DEPENDS` — env var exported per-component in package_all.sh before envsubst
- `COMPONENT_BINARIES` — new associative array in package_all.sh mapping each component to its DESTDIR-relative binary path(s)

Symbols REMOVED by this plan (expected absences, not drift):
- `detect_crun_parser_depend()` and call sites (package_all.sh:199-219, 366-371) — absorbed by detect_runtime_depends (D-04)
- `CRUN_PARSER_DEPEND` env var and `${CRUN_PARSER_DEPEND}` placeholder
- `VERSION_SUFFIX="~podman1"` literal (package_all.sh:27) — now sourced from config.sh (Plan 01)

Consumed from Plan 01 (already exist, do not redefine):
- `detect_runtime_depends()`, `detect_distro_version_id()` in functions.sh
- `VERSION_SUFFIX` exported by config.sh
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewire package_all.sh — remove hardcoded suffix + crun special case, add per-component detection</name>
  <files>scripts/package_all.sh</files>
  <read_first>
    - scripts/package_all.sh (FULL — current state. Sites: line 27 `VERSION_SUFFIX="~podman1"`; lines 199-219 detect_crun_parser_depend(); lines 319-385 packaging loop; lines 360-379 per-component export+envsubst with CRUN_PARSER_DEPEND special case 366-371 and envsubst allowlist 374; lines 392-409 suite block. The `${VERSION_SUFFIX}` append sites 344/355/397/400 need NO change.)
    - config.sh (Plan 01 output — VERSION_SUFFIX exported here; package_all.sh sources config.sh at line 11, so it is in scope)
    - functions.sh (Plan 01 output — detect_runtime_depends takes one-or-more absolute binary paths, prints owning package names one-per-line; sourced at package_all.sh line 14)
    - .planning/phases/19-per-distro-versioning-dependency-mapping/19-PATTERNS.md (lines 87-110 — three concrete edit sites + new per-component export shape)
    - .planning/phases/19-per-distro-versioning-dependency-mapping/19-RESEARCH.md (Pattern 3 lines 233-251 sed fragment shape; anti-patterns 253-258 pasta passt+pasta + empty-deps; Pitfall 3 304-308 empty-list/indentation)
  </read_first>
  <action>
    Make four edits to package_all.sh:

    (1) DELETE line 27 `VERSION_SUFFIX="~podman1"`. VERSION_SUFFIX now comes from config.sh (sourced line 11, Plan 01). The four `${VERSION_SUFFIX}` append sites (344, 355, 397, 400) keep working unchanged — they now append the per-distro value.

    (2) DELETE the entire detect_crun_parser_depend() function (lines 199-219) (D-04).

    (3) Add a per-component binary-path map near the COMPONENTS array (around line 275): `declare -A COMPONENT_BINARIES` mapping each component that ships native ELF binaries to its DESTDIR-relative path(s). Values: podman=`usr/bin/podman usr/bin/podman-remote`; crun=`usr/bin/crun`; conmon=`usr/bin/conmon`; buildah=`usr/bin/buildah`; skopeo=`usr/bin/skopeo`; pasta=`usr/bin/passt usr/bin/pasta` (two binaries — RESEARCH line 258); netavark=`usr/bin/netavark`; aardvark-dns=`usr/bin/aardvark-dns`; fuse-overlayfs=`usr/bin/fuse-overlayfs`; catatonit=`usr/bin/catatonit`. container-configs and toolbox ship no native ELF binary → no entry (detection skipped). Space-separated string value per key for multi-binary components.

    (4) REPLACE the per-component export+envsubst block (lines 360-379). Remove `export CRUN_PARSER_DEPEND="libyajl2"` and the `if [[ "${component}" == "crun" ]]` special case (366-371). In their place: if the component has a COMPONENT_BINARIES entry, expand each path to `${DESTDIR}/<path>` and call `DETECTED_DEPENDS="$(detect_runtime_depends <abs paths...> | sed 's/^/  - /')"` to produce a YAML list fragment (each line `  - pkgname`, two-space indent + `- `), then `export DETECTED_DEPENDS`. If the component has NO binary entry, set `DETECTED_DEPENDS=""` and export it. Change the envsubst allowlist from `'${VERSION} ${ARCH} ${DESTDIR} ${CRUN_PARSER_DEPEND}'` to `'${VERSION} ${ARCH} ${DESTDIR} ${DETECTED_DEPENDS}'`. detect_runtime_depends returns 1 on any unmapped lib; under `set -euo pipefail` + ERR trap that aborts the build — no `|| true`, no fallback (D-03).

    The suite meta-package block (392-419) needs no DETECTED_DEPENDS (suite.yaml has only internal deps) — leave its envsubst allowlist as-is.

    Preserve all existing behavior: edge tag resolution, nightly extraction, pasta date-based version, loop order, output summary. The ONLY semantic changes: suffix source, crun special-case removal, per-component detected-depends injection.
  </action>
  <verify>
    <automated>bash -n scripts/package_all.sh && ! grep -q 'detect_crun_parser_depend' scripts/package_all.sh && ! grep -q 'CRUN_PARSER_DEPEND' scripts/package_all.sh && ! grep -qE '^[[:space:]]*VERSION_SUFFIX="~podman1"' scripts/package_all.sh && grep -q 'detect_runtime_depends' scripts/package_all.sh && grep -q 'DETECTED_DEPENDS' scripts/package_all.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/package_all.sh` exits 0
    - `grep -c 'detect_crun_parser_depend' scripts/package_all.sh` returns 0
    - `grep -c 'CRUN_PARSER_DEPEND' scripts/package_all.sh` returns 0
    - `grep -cE '^[[:space:]]*VERSION_SUFFIX="~podman1"' scripts/package_all.sh` returns 0
    - `grep -c 'detect_runtime_depends' scripts/package_all.sh` is at least 1
    - package_all.sh contains `declare -A COMPONENT_BINARIES` with a `pasta` entry referencing both `passt` and `pasta`
    - package_all.sh component envsubst line contains `${DETECTED_DEPENDS}` and not `${CRUN_PARSER_DEPEND}`
    - `grep -c '${VERSION_SUFFIX}' scripts/package_all.sh` is at least 4 (append sites intact)
  </acceptance_criteria>
  <done>
    package_all.sh: hardcoded suffix removed (config.sh authoritative); detect_crun_parser_depend + CRUN_PARSER_DEPEND fully gone; COMPONENT_BINARIES map added; per-component DETECTED_DEPENDS computed via detect_runtime_depends and injected through the envsubst allowlist; hard-fail preserved under set -euo pipefail. `bash -n` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert nFPM YAMLs to ${DETECTED_DEPENDS} injection (drop hardcoded system libs, keep internal deps)</name>
  <files>packaging/nfpm/podman.yaml, packaging/nfpm/buildah.yaml, packaging/nfpm/skopeo.yaml, packaging/nfpm/crun.yaml, packaging/nfpm/conmon.yaml, packaging/nfpm/pasta.yaml</files>
  <read_first>
    - packaging/nfpm/crun.yaml (depends 14-19: libseccomp2, libsystemd0, libcap2, ${CRUN_PARSER_DEPEND} — all system libs, no internal deps)
    - packaging/nfpm/podman.yaml (depends 14-26: internal podman-* 16-23 KEEP; libgpgme11, libseccomp2 25-26 REMOVE)
    - packaging/nfpm/buildah.yaml (14-18: podman-container-configs KEEP; libgpgme11, libseccomp2 REMOVE)
    - packaging/nfpm/skopeo.yaml (14-19: podman-container-configs KEEP; libgpgme11, libsubid4, libsqlite3-0 REMOVE)
    - packaging/nfpm/conmon.yaml (14-17: libglib2.0-0, libsystemd0 — all system libs, no internal deps)
    - packaging/nfpm/pasta.yaml (NO depends: key today — a depends block with ${DETECTED_DEPENDS} must be ADDED)
    - .planning/phases/19-per-distro-versioning-dependency-mapping/19-PATTERNS.md (lines 114-132 — per-file current state + edit shape + YAML caveat)
    - .planning/phases/19-per-distro-versioning-dependency-mapping/19-RESEARCH.md (Pattern 3 237-251 placeholder at column 0; Pitfall 3 304-308 empty-list)
  </read_first>
  <action>
    Edit the 6 nFPM YAMLs so system-library deps come from `${DETECTED_DEPENDS}` (D-12) while internal `podman-*` deps stay literal (D-13). The placeholder sits at column 0 (no indentation) under `depends:`, because injected fragment lines already carry their own `  - ` two-space indent (RESEARCH Pattern 3 + Pitfall 3 caveat).

    - crun.yaml: replace the four system-lib lines (libseccomp2, libsystemd0, libcap2, ${CRUN_PARSER_DEPEND}) with one `${DETECTED_DEPENDS}` at column 0 under `depends:`. No internal deps.
    - podman.yaml: KEEP the 8 internal `- podman-*` lines literal (podman-crun, podman-conmon, podman-netavark, podman-aardvark-dns, podman-pasta, podman-fuse-overlayfs, podman-catatonit, podman-container-configs). DELETE `- libgpgme11`, `- libseccomp2`. Add `${DETECTED_DEPENDS}` at column 0 after the last internal dep line.
    - buildah.yaml: KEEP `- podman-container-configs`. DELETE `- libgpgme11`, `- libseccomp2`. Add `${DETECTED_DEPENDS}` at column 0 after it.
    - skopeo.yaml: KEEP `- podman-container-configs`. DELETE `- libgpgme11`, `- libsubid4`, `- libsqlite3-0`. Add `${DETECTED_DEPENDS}` at column 0 after it.
    - conmon.yaml: replace both system-lib lines (libglib2.0-0, libsystemd0) with `${DETECTED_DEPENDS}` at column 0. No internal deps.
    - pasta.yaml: ADD a new `depends:` block (currently none) with `${DETECTED_DEPENDS}` at column 0, placed after the header fields and before `conflicts:`.

    Update the existing "runtime deps verified via ldd /usr/bin/<x>" comments to say deps are detected and injected at build time (keep short). Do NOT touch contents:, conflicts:, replaces:, provides:.

    Empty-list edge case (Pitfall 3): for these 6 components a non-empty detected set is expected (crun/conmon/pasta are native C; podman/buildah/skopeo are cgo→gpgme), so an empty `depends:` is not expected — Plan 04's render-and-parse smoke catches any empty/garbled block. Do NOT edit netavark, aardvark-dns, fuse-overlayfs, catatonit, toolbox, or container-configs in this task: netavark/aardvark-dns keep their single internal dep with no system-lib injection point; fuse-overlayfs/catatonit declare no deps and stay unchanged (Plan 04 surfaces their real detected set, if any, and only then is a block added — do not pre-add an empty block).
  </action>
  <verify>
    <automated>for f in crun podman buildah skopeo conmon pasta; do grep -q 'DETECTED_DEPENDS' "packaging/nfpm/$f.yaml" || { echo "FAIL $f"; exit 1; }; done && ! grep -rqE '^[[:space:]]*-[[:space:]]*(libgpgme11|libseccomp2|libsystemd0|libcap2|libglib2\.0-0|libsubid4|libsqlite3-0)([[:space:]]|$)' packaging/nfpm/ && ! grep -rq 'CRUN_PARSER_DEPEND' packaging/nfpm/ && grep -q 'podman-container-configs' packaging/nfpm/skopeo.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of crun/podman/buildah/skopeo/conmon/pasta YAML contains `${DETECTED_DEPENDS}`
    - No nFPM YAML contains a literal `- libgpgme11`, `- libseccomp2`, `- libsystemd0`, `- libcap2`, `- libglib2.0-0`, `- libsubid4`, or `- libsqlite3-0` line
    - No nFPM YAML contains `CRUN_PARSER_DEPEND`
    - podman.yaml still lists all 8 internal `- podman-*` deps; skopeo.yaml + buildah.yaml still list `- podman-container-configs`
    - pasta.yaml now has a `depends:` key containing `${DETECTED_DEPENDS}`
    - `${DETECTED_DEPENDS}` is at column 0 in each edited YAML (`grep -nE '^\$\{DETECTED_DEPENDS\}' packaging/nfpm/crun.yaml` returns a match)
  </acceptance_criteria>
  <done>
    All 6 target YAMLs inject `${DETECTED_DEPENDS}` at column 0 under `depends:`; hardcoded system-lib lines removed (D-12); internal podman-* deps preserved literal (D-13); pasta gains a depends block; CRUN_PARSER_DEPEND placeholder gone everywhere.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build host package DB → .deb control metadata | `detect_runtime_depends` output (owning package names from `dpkg-query`) is injected verbatim into the nFPM `depends:` block and thus the published `.deb` control file |
| rendered /tmp/nfpm-*.yaml → nfpm | the envsubst-rendered config (containing the injected fragment) is fed to `nfpm pkg` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering / Integrity | ${DETECTED_DEPENDS} injection into nFPM YAML | mitigate | Detected names come only from the build host's own package DB via dpkg-query (not network); the fragment is built with `sed 's/^/  - /'` from sorted-unique names; malformed/empty YAML is caught by `nfpm pkg` parsing loudly (validated in Plan 04 render-and-parse smoke). No external input reaches the depends block. |
| T-19-06 | Repudiation / Integrity | silent fallback masking a mislinked binary | mitigate | D-03 hard-fail inherited from Plan 01's detect_runtime_depends; package_all.sh uses no `|| true` around the detection call, so an unmapped soname aborts the build under set -euo pipefail. |
| T-19-07 | Tampering | unquoted DESTDIR/binary-path expansion in detection call | mitigate | All `${DESTDIR}/<path>` expansions quoted; COMPONENT_BINARIES values are static, repo-controlled strings (no external input); `set -euo pipefail` project-wide. |
</threat_model>

<verification>
- `bash -n scripts/package_all.sh` passes; crun special case + hardcoded suffix fully removed (grep == 0).
- All 6 target YAMLs carry `${DETECTED_DEPENDS}`; no hardcoded system-lib dep line remains anywhere in packaging/nfpm/.
- Internal podman-* deps preserved (D-13).
- Note: end-to-end proof (real detection on built binaries, rendered-YAML parse, .deb Depends field) requires an Ubuntu build host and is exercised in Plan 04. This plan's automated checks are syntax + grep structural assertions, runnable on the dev host.
</verification>

<success_criteria>
- package_all.sh computes per-component system-lib deps from binaries via detect_runtime_depends and injects them through the existing envsubst step; no crun special case; no hardcoded suffix (PKG-10 mechanism wired).
- The 6 nFPM YAMLs declare system deps exclusively via `${DETECTED_DEPENDS}` while internal suite deps remain literal (D-12/D-13).
</success_criteria>

<output>
Create `.planning/phases/19-per-distro-versioning-dependency-mapping/19-02-SUMMARY.md` when done.
</output>
