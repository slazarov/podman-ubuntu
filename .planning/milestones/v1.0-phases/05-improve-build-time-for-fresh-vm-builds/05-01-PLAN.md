---
phase: 05-improve-build-time-for-fresh-vm-builds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-04
status: completed
completion_date: 2026-03-03
must_haves:
  truths:
    - "All Make-based builds use parallel compilation (-j flag with NPROC)"
    - "Git clones use shallow clone (--depth 1) reducing network transfer by ~95%"
    - "Cargo builds use CARGO_BUILD_JOBS for 2-4x parallel speedup"
    - "Users can override all optimization settings via environment variables"
  artifacts:
    - path: "config.sh"
      provides: "All optimization variables"
      contains: "NPROC"
    - path: "config.sh"
      provides: "Shallow clone configuration"
      contains: "SHALLOW_CLONE"
    - path: "config.sh"
      provides: "Cargo parallelization"
      contains: "CARGO_BUILD_JOBS"
    - path: "functions.sh"
      provides: "git_clone_update with shallow clone support"
      contains: "git clone --depth 1"
    - path: "scripts/build_*.sh"
      contains: 'make -j "\$NPROC"'
  key_links:
    - from: "config.sh"
      to: "all build scripts"
      via: "source"
      pattern: "NPROC"
---

<objective>
COMPLETED: Foundation for build optimization was already implemented.

This plan is marked complete because the following work was already done:
- NPROC detection in config.sh
- SHALLOW_CLONE in config.sh and functions.sh
- Parallel make (-j "$NPROC") in all 11 build scripts
- CARGO_BUILD_JOBS and SCCACHE_ENABLED in config.sh

Remaining work (PERF-03 Go optimization for 5 scripts) is in 05-02-PLAN.md.

Purpose: Historical reference for completed work.
Output: N/A (already implemented)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Verification of completed work

Run these commands to verify completion:
```bash
# NPROC and SHALLOW_CLONE in config.sh
grep -E 'export (NPROC|SHALLOW_CLONE)=' config.sh

# Shallow clone in functions.sh
grep 'git clone --depth 1' functions.sh

# Parallel make in all build scripts
grep -l 'make -j "\$NPROC"' scripts/build_*.sh | wc -l  # Should be 11

# Cargo optimization in config.sh
grep 'CARGO_BUILD_JOBS' config.sh
```
</context>

<tasks>
<!-- No tasks - work already completed -->
<task type="auto">
  <name>Task 1: Verify NPROC and SHALLOW_CLONE in config.sh</name>
  <files>config.sh</files>
  <action>Verify only - variables already exist</action>
  <verify>
    <automated>grep -q 'export NPROC="\${NPROC:-\$(nproc)}"' config.sh && grep -q 'export SHALLOW_CLONE="\${SHALLOW_CLONE:-true}"' config.sh</automated>
  </verify>
  <done>NPROC and SHALLOW_CLONE variables confirmed in config.sh</done>
</task>

<task type="auto">
  <name>Task 2: Verify shallow clone in functions.sh</name>
  <files>functions.sh</files>
  <action>Verify only - shallow clone logic already exists</action>
  <verify>
    <automated>grep -q 'SHALLOW_CLONE:-true' functions.sh && grep -q 'git clone --depth 1' functions.sh</automated>
  </verify>
  <done>git_clone_update applies shallow clone for fresh clones</done>
</task>

<task type="auto">
  <name>Task 3: Verify parallel make in all build scripts</name>
  <files>scripts/build_*.sh</files>
  <action>Verify only - all scripts already use make -j "$NPROC"</action>
  <verify>
    <automated>test $(grep -l 'make -j "\$NPROC"' scripts/build_*.sh | wc -l) -eq 11</automated>
  </verify>
  <done>All 11 Make-based build scripts use parallel make</done>
</task>

<task type="auto">
  <name>Task 4: Verify Cargo optimization in config.sh</name>
  <files>config.sh</files>
  <action>Verify only - CARGO_BUILD_JOBS and SCCACHE_ENABLED already exist</action>
  <verify>
    <automated>grep -q 'export CARGO_BUILD_JOBS="\${CARGO_BUILD_JOBS:-\$NPROC}"' config.sh && grep -q 'export SCCACHE_ENABLED="\${SCCACHE_ENABLED:-false}"' config.sh</automated>
  </verify>
  <done>Cargo optimization variables confirmed in config.sh</done>
</task>
</tasks>

<verification>
All requirements PERF-01, PERF-02, PERF-04 are already implemented.
Run verification commands in context section to confirm.
</verification>

<success_criteria>
- [x] NPROC variable defined in config.sh with nproc default
- [x] SHALLOW_CLONE variable defined in config.sh with true default
- [x] git_clone_update uses --depth 1 for fresh clones when SHALLOW_CLONE=true
- [x] All 11 Make-based build scripts use make -j "$NPROC"
- [x] CARGO_BUILD_JOBS variable defined in config.sh
- [x] SCCACHE_ENABLED variable defined in config.sh
</success_criteria>

<output>
This plan is complete. No SUMMARY needed for verification-only plans.
</output>
