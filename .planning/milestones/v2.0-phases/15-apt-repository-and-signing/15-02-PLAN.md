---
phase: 15-apt-repository-and-signing
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - packaging/repo/pubkey.gpg
autonomous: false
requirements: [REPO-02, REPO-05]
user_setup:
  - service: gpg
    why: "Ed25519 signing key for APT repository"
    env_vars:
      - name: GPG_PRIVATE_KEY
        source: "Generated locally, stored as GitHub Secret (Settings -> Secrets and variables -> Actions -> New repository secret)"
    dashboard_config:
      - task: "Add GPG_PRIVATE_KEY repository secret"
        location: "GitHub -> Repository Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "Public GPG key file exists in repository for user download"
    - "GPG private key is stored as a GitHub Actions secret for CI signing"
    - "repo_manage.sh produces valid InRelease and Release.gpg when run with the generated key"
  artifacts:
    - path: "packaging/repo/pubkey.gpg"
      provides: "Binary Ed25519 public GPG key"
    - path: "scripts/repo_manage.sh"
      provides: "Repository management script (created in plan 01)"
  key_links:
    - from: "packaging/repo/pubkey.gpg"
      to: "scripts/repo_manage.sh"
      via: "script copies pubkey.gpg to repo root as podman-debian.gpg"
      pattern: "pubkey.gpg"
---

<objective>
Generate Ed25519 GPG signing key, commit public key to repository, and store private key as a GitHub Secret for CI use.

Purpose: The APT repository requires a GPG signing key to produce valid InRelease and Release.gpg files. Without this key, `apt update` would require `--allow-insecure-repositories` which is unacceptable.
Output: Public GPG key committed, private key in GitHub Secrets, verified signing workflow
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-apt-repository-and-signing/15-RESEARCH.md
@.planning/phases/15-apt-repository-and-signing/15-01-SUMMARY.md

<interfaces>
<!-- From plan 15-01 -->
From scripts/repo_manage.sh:
- Accepts GPG_PRIVATE_KEY env var for CI key import
- Falls back to pubkey.gpg in packaging/repo/ for public key export
- Invokes reprepro with SignWith: yes (uses default key in keyring)

From packaging/repo/conf/distributions:
- SignWith: yes (requires at least one secret key in GPG keyring)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Ed25519 GPG key and export public key</name>
  <files>packaging/repo/pubkey.gpg</files>
  <action>
Generate an Ed25519 GPG signing key and commit the public key to the repository.

**Key generation (run on the local machine):**
```bash
# Generate Ed25519 key without passphrase (required for CI automation)
gpg --batch --passphrase '' --quick-gen-key \
  "Podman Debian <podman-debian@users.noreply.github.com>" \
  ed25519 default 0

# Get the fingerprint
GPG_KEY_ID=$(gpg --list-keys --with-colons "podman-debian@users.noreply.github.com" \
  | grep fpr | head -1 | cut -d: -f10)

echo "GPG Key Fingerprint: ${GPG_KEY_ID}"
```

**Export public key (binary format -- NOT ASCII armor):**
```bash
gpg --export "${GPG_KEY_ID}" > packaging/repo/pubkey.gpg
```

Verify the file is binary (not ASCII-armored): it should NOT start with "-----BEGIN". APT's `signed-by` directive requires binary format.

**Export private key for GitHub Secret (DO NOT COMMIT THIS):**
```bash
gpg --armor --export-secret-keys "${GPG_KEY_ID}"
```

Print the ASCII-armored private key to stdout so the user can copy it to GitHub Secrets in the next task. Do NOT write it to a file.

**Important:** If a key for "podman-debian@users.noreply.github.com" already exists in the keyring, skip generation and use the existing key.
  </action>
  <verify>
    <automated>test -f packaging/repo/pubkey.gpg && test -s packaging/repo/pubkey.gpg && ! head -c 30 packaging/repo/pubkey.gpg | grep -q "BEGIN"</automated>
  </verify>
  <done>Binary public GPG key exists at packaging/repo/pubkey.gpg, is non-empty, and is in binary format (not ASCII-armored)</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Store GPG private key in GitHub Secrets</name>
  <action>
The GPG private key must be stored as a GitHub Actions secret. Claude cannot access GitHub repository settings.
  </action>
  <instructions>
1. Copy the ASCII-armored private key output from Task 1
2. Go to: https://github.com/slazarov/podman-debian/settings/secrets/actions
3. Click "New repository secret"
4. Name: GPG_PRIVATE_KEY
5. Value: Paste the entire ASCII-armored private key (including -----BEGIN/END lines)
6. Click "Add secret"
  </instructions>
  <resume-signal>Type "done" once the GPG_PRIVATE_KEY secret is saved in GitHub</resume-signal>
</task>

</tasks>

<verification>
- packaging/repo/pubkey.gpg exists and is binary format
- GPG_PRIVATE_KEY secret exists in GitHub repository settings (confirmed by user)
- Key pair matches: public key in repo corresponds to private key in GitHub Secrets
</verification>

<success_criteria>
- Ed25519 GPG key pair generated
- Public key committed to repository as binary .gpg file
- Private key stored in GitHub Actions secrets
- Key is passphrase-less for CI automation
</success_criteria>

<output>
After completion, create `.planning/phases/15-apt-repository-and-signing/15-02-SUMMARY.md`
</output>
